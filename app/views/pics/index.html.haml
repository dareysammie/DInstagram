.col-md-6.col-md-offset-3
	#pics.transitions-enabled.centered
		- @pics.each do |pic|
			.panel.panel-default
				%p= pic.user.email
				= link_to (image_tag pic.image.url), pic
				%p= pic.title
				
				
				
	
			
    
